export function AppShell({ children }: { children: React.ReactNode }) {
  return <div className="app-shell">{children}</div>;
}

export function AppNavigation() {
  return <nav className="app-navigation"></nav>;
}
